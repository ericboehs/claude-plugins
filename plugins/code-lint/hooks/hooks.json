{
  "description": "Automatic linting on file edit/write",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/lint.sh"
          }
        ]
      }
    ]
  }
}
